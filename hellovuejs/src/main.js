import Vue from 'vue'
import App from './App.vue'

new Vue({
  el: '#app',
  render: h => h(App)
})

// Mỗi thành phần trong trang Web sẽ tương ứng với mỗi Component, và mỗi component này nó sẽ tương ứng với 1 file *.vue